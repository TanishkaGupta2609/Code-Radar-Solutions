#include <stdio.h>
#include <string.h>
void PanagramString(char arr[]){
    int arr=arr_int[]
    int flag=0;
    for(int i=0;i<n;i++){
        if(arr_int[i]>=65 && arr_int[i]<90 ||arr_int[i]>=97 && arr_int[i]<122){
            flag=1;
        }
    }
    if(flag){
        printf("Yes");
    }
    else{
        printf("No");
    }
}
int main(){
    char arr[500];
    fgets(arr,500,stdin);
    PanagramString(arr);
    return 0;
}